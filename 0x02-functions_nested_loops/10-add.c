#include "main.h"

/**-add.c - adds two integers
 * Returns the results
 * @a and b : are the parameters
 */
int add(int a, int b)
{
	int sum = a + b;
	return sum;
}
int main(void)
{
	int n;
	n = add(89, 9);
	_putchar(sum);
	return (0);
}
